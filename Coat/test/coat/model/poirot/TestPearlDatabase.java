package coat.model.poirot;

/**
 * @author Lorente Arencibia, Pascual (pasculorente@gmail.com)
 */
public class TestPearlDatabase {
/*
    @Test
    public void withContains() {
        // Given
        PearlDatabase database = new PearlDatabase(null);
        Pearl pearl = new Pearl("MUC1", "gene");
        // When
        database.add(pearl);
        // Then
        Assert.assertTrue(database.contains(pearl));
    }

    @Test
    public void withNotContains() {
        // Given
        PearlDatabase database = new PearlDatabase(null);
        Pearl pearl = new Pearl("MUC1", "gene");
        // Then
        Assert.assertFalse(database.contains(pearl));
    }

    @Test
    public void testGet() {
        // Given
        PearlDatabase database = new PearlDatabase(null);
        Pearl pearl = new Pearl("NDEL1", "gene");
        // When
        database.add(pearl);
        // Then
        Assert.assertEquals(pearl, database.getPearl("NDEL1"));
    }

    @Test
    public void withSize() {
        // When
        PearlDatabase database = new PearlDatabase(null);
        // Then
        Assert.assertEquals(0, database.size());
        // And when
        database.add(new Pearl("id", "gene"));
        // Then
        Assert.assertEquals(1, database.size());
        // And when
        for (int i = 0; i < 10; i++) database.add(new Pearl(i + "", "gene"));
        // Then
        Assert.assertEquals(11, database.size());
    }
*/
}
